Prova do fix do bug do resolveSendCredentials, saida bruta, sem edicao.
Script: backend/verify-tmp.ts (fica so nesta branch, nao vai para o dist --
backend/tsconfig.json inclui so src/**/*).

===================== ANTES DO FIX (codigo de origin/master) =====================
===== CASO 1: phoneNumberId bate com conta ATIVA -- usa a credencial da conta =====
  resultado: nao lancou (enviou)
  >>> ESPERADO: nao lanca -- usa a credencial da conta PN_ATIVA_123.

===== CASO 2: tabela de contas VAZIA -- fallback pro env e o comportamento certo hoje =====
  resultado: nao lancou (enviou, via env)
  >>> ESPERADO: nao lanca -- tabela vazia = modo legado, env e a unica credencial que existe.

===== CASO 3 (O BUG): existe conta na tabela, mas esta phoneNumberId nao bate -- NAO cai pro env =====
  resultado: nao lancou (ENVIOU PELO NUMERO ERRADO -- BUG)
  >>> ESPERADO: lanca erro. Antes do fix: caia pro env, mandando pelo numero global em silencio.

===== CASO 3b: mesma conta, mas DESATIVADA (active=false) -- cenario mais provavel em producao =====
  resultado: nao lancou (ENVIOU PELO NUMERO ERRADO -- BUG)
  >>> ESPERADO: lanca erro -- mesma conta existe, so que desativada, e nao pode cair pro env.

===================== DEPOIS DO FIX =====================
===== CASO 1: phoneNumberId bate com conta ATIVA -- usa a credencial da conta =====
  resultado: nao lancou (enviou)
  >>> ESPERADO: nao lanca -- usa a credencial da conta PN_ATIVA_123.

===== CASO 2: tabela de contas VAZIA -- fallback pro env e o comportamento certo hoje =====
  resultado: nao lancou (enviou, via env)
  >>> ESPERADO: nao lanca -- tabela vazia = modo legado, env e a unica credencial que existe.

===== CASO 3 (O BUG): existe conta na tabela, mas esta phoneNumberId nao bate -- NAO cai pro env =====
  resultado: LANCOU: Nenhuma conta WhatsApp ativa para phoneNumberId=PN_DESCONHECIDA_OU_DESATIVADA -- conta inexistente ou desativada. Não caio para as credenciais globais: enviaria pelo número errado em silêncio.
  >>> ESPERADO: lanca erro. Antes do fix: caia pro env, mandando pelo numero global em silencio.

===== CASO 3b: mesma conta, mas DESATIVADA (active=false) -- cenario mais provavel em producao =====
  resultado: LANCOU: Nenhuma conta WhatsApp ativa para phoneNumberId=PN_DESATIVADA -- conta inexistente ou desativada. Não caio para as credenciais globais: enviaria pelo número errado em silêncio.
  >>> ESPERADO: lanca erro -- mesma conta existe, so que desativada, e nao pode cair pro env.
